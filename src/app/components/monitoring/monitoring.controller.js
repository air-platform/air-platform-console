/**
 * Created by Otherplayer on 16/7/21.
 */
(function() {
    'use strict';

    angular.module('iot').controller('MonitoringController', MonitoringController);

    /** @ngInject */
    function MonitoringController($rootScope, $timeout, StorageService,$state, $uibModal, UserInfoServer, $log, NetworkService, constdata, toastr) {
        /* jshint validthis: true */
        var vm = this;
        
    }
})();
